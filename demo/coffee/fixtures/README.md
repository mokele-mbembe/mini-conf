# Coffee Demo Fixtures

## Templates

### `templates/coffee-main.toml`

Default content for the `coffee-main` config file seeded onto the template instance.

### `stores/`

Per-instance seed overrides (currently handled by `manifest.yaml` inline content).
Files here document the intended fixture state for reference.

## Client Config Files

See `../clients/` for local config files read by `demo-coffee-client`.
