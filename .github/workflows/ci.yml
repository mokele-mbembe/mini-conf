name: CI

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      alpha: ${{ steps.filter.outputs.alpha }}
      frontend: ${{ steps.filter.outputs.frontend }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Filter alpha-relevant changes
        id: filter
        uses: dorny/paths-filter@v4.0.1
        with:
          filters: |
            alpha:
              - 'apps/server/**'
              - 'crates/**'
              - 'migrations/**'
              - 'scripts/**'
              - 'justfile'
              - '.github/workflows/ci.yml'
              - 'docs/artifacts/openapi.json'
              - 'tests/alpha/**'
            frontend:
              - 'apps/web/**'
              - 'apps/server/**'
              - 'crates/**'
              - 'migrations/**'
              - 'justfile'
              - 'scripts/**'
              - 'package.json'
              - 'pnpm-lock.yaml'
              - 'pnpm-workspace.yaml'
              - '.github/workflows/ci.yml'

  quality:
    if: ${{ github.event_name != 'workflow_dispatch' }}
    needs: changes
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/postgres
      TEST_DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/postgres
      PERF_SMOKE_MAX_MS: 100
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install just
        uses: extractions/setup-just@v4

      - name: Set up Rust
        if: ${{ hashFiles('Cargo.toml') != '' }}
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache Rust
        if: ${{ hashFiles('Cargo.toml') != '' }}
        uses: Swatinem/rust-cache@v2

      - name: Install sqlx-cli
        if: ${{ hashFiles('Cargo.toml') != '' }}
        uses: baptiste0928/cargo-install@v3
        with:
          crate: sqlx-cli
          args: --no-default-features --features rustls,postgres

      - name: Install cargo-nextest
        if: ${{ hashFiles('Cargo.toml') != '' }}
        uses: baptiste0928/cargo-install@v3
        with:
          crate: cargo-nextest
          locked: true

      - name: Install cargo-llvm-cov
        if: ${{ hashFiles('Cargo.toml') != '' }}
        uses: baptiste0928/cargo-install@v3
        with:
          crate: cargo-llvm-cov
          locked: true

      - name: Set up pnpm
        if: ${{ hashFiles('package.json', 'pnpm-workspace.yaml', 'apps/web/package.json') != '' }}
        uses: pnpm/action-setup@v5

      - name: Set up Node
        if: ${{ hashFiles('package.json', 'pnpm-workspace.yaml', 'apps/web/package.json') != '' }}
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: pnpm

      - name: Install frontend dependencies
        if: ${{ hashFiles('pnpm-lock.yaml') != '' }}
        run: pnpm install --frozen-lockfile

      - name: Install apps/web dependencies
        if: ${{ hashFiles('pnpm-lock.yaml') == '' && hashFiles('apps/web/package.json') != '' }}
        run: pnpm --dir apps/web install --no-frozen-lockfile

      - name: Frontend build check
        if: ${{ hashFiles('apps/web/package.json') != '' }}
        run: pnpm --dir apps/web build

      - name: Lint
        run: just lint

      - name: SQLx check
        run: just sqlx-check

      - name: OpenAPI check
        run: just openapi-check

      - name: Test
        run: just test

      - name: Coverage baseline
        run: just coverage-check

      - name: Performance smoke
        run: just perf-smoke

      - name: Upload perf smoke artifact
        if: ${{ always() && hashFiles('target/perf/smoke.json') != '' }}
        uses: actions/upload-artifact@v6
        with:
          name: perf-smoke
          path: target/perf/smoke.json

  backend-db:
    if: ${{ github.event_name != 'workflow_dispatch' }}
    needs: changes
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/postgres
      TEST_DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/postgres
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install just
        uses: extractions/setup-just@v4

      - name: Set up Rust
        if: ${{ hashFiles('Cargo.toml') != '' }}
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        if: ${{ hashFiles('Cargo.toml') != '' }}
        uses: Swatinem/rust-cache@v2

      - name: Install sqlx-cli
        if: ${{ hashFiles('Cargo.toml') != '' }}
        uses: baptiste0928/cargo-install@v3
        with:
          crate: sqlx-cli
          args: --no-default-features --features rustls,postgres

      - name: Install cargo-nextest
        if: ${{ hashFiles('Cargo.toml') != '' }}
        uses: baptiste0928/cargo-install@v3
        with:
          crate: cargo-nextest
          locked: true

      - name: Run migrations
        run: just db-migrate-up

      - name: Run backend DB tests
        run: just test-backend-db

      - name: Clean leftover backend DB test schemas
        if: ${{ always() }}
        continue-on-error: true
        run: bash scripts/db-clean-test-schemas.sh --apply

  alpha-smoke:
    if: ${{ github.event_name == 'pull_request' && needs.changes.outputs.alpha == 'true' }}
    needs: changes
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/postgres
      TEST_DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/postgres
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install just
        uses: extractions/setup-just@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Install sqlx-cli
        uses: baptiste0928/cargo-install@v3
        with:
          crate: sqlx-cli
          args: --no-default-features --features rustls,postgres

      - name: Install Hurl
        run: |
          VERSION=7.0.0
          curl --fail --silent --show-error --location \
            "https://github.com/Orange-OpenSource/hurl/releases/download/${VERSION}/hurl-${VERSION}-x86_64-unknown-linux-gnu.tar.gz" \
            | tar xz -C "${RUNNER_TEMP}"
          echo "${RUNNER_TEMP}/hurl-${VERSION}-x86_64-unknown-linux-gnu/bin" >> "${GITHUB_PATH}"

      - name: Build server
        run: cargo build --bin server

      - name: Run alpha smoke HTTP tests
        run: just alpha-smoke

  alpha-full:
    if: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.changes.outputs.alpha == 'true') || github.event_name == 'workflow_dispatch' }}
    needs: changes
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/postgres
      TEST_DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/postgres
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install just
        uses: extractions/setup-just@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Install sqlx-cli
        uses: baptiste0928/cargo-install@v3
        with:
          crate: sqlx-cli
          args: --no-default-features --features rustls,postgres

      - name: Install Hurl
        run: |
          VERSION=7.0.0
          curl --fail --silent --show-error --location \
            "https://github.com/Orange-OpenSource/hurl/releases/download/${VERSION}/hurl-${VERSION}-x86_64-unknown-linux-gnu.tar.gz" \
            | tar xz -C "${RUNNER_TEMP}"
          echo "${RUNNER_TEMP}/hurl-${VERSION}-x86_64-unknown-linux-gnu/bin" >> "${GITHUB_PATH}"

      - name: Build server
        run: cargo build --bin server

      - name: Run alpha full HTTP tests
        run: just alpha-full

  frontend-smoke:
    if: ${{ ((github.event_name == 'pull_request') || (github.event_name == 'push' && github.ref == 'refs/heads/main')) && needs.changes.outputs.frontend == 'true' }}
    needs: changes
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      TEST_DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/postgres
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install just
        uses: extractions/setup-just@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Install sqlx-cli
        uses: baptiste0928/cargo-install@v3
        with:
          crate: sqlx-cli
          args: --no-default-features --features rustls,postgres

      - name: Set up pnpm
        uses: pnpm/action-setup@v5

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: pnpm

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Build frontend
        run: pnpm --dir apps/web build

      - name: Install Playwright browsers
        run: pnpm --dir apps/web exec playwright install --with-deps chromium

      - name: Build server
        run: cargo build --bin server

      - name: Run isolated Playwright smoke
        run: just test-e2e

      - name: Clean leftover test schemas
        if: ${{ always() }}
        continue-on-error: true
        run: bash scripts/db-clean-test-schemas.sh --apply
